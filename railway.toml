[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1 --ws-max-size 10485760 --timeout-keep-alive 120"
healthcheckPath = "/api/state"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
